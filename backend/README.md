# Backend Structure

This folder contains the planned FastAPI backend structure for NutriCore AI.

The backend is organized by:

- `api/` for routers
- `schemas/` for request and response contracts
- `models/` for database entities
- `services/` for orchestration
- `engines/` for deterministic health logic
- `repositories/` for persistence access
- `workers/` for asynchronous AI jobs
